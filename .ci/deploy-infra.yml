---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: latest

inputs:
- name: selfhydro-ui

run:
  path: selfhydro-ui/.ci/build-infra.sh

params:
  GCP-KEY: ((gcpServiceAccount.json))
