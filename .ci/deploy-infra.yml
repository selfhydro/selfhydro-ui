---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: hashicorp/terraform
    tag: light

inputs:
- name: selfhydro-ui

run:
  path: selfhydro-ui/.ci/deploy-infra.sh

params:
  GCP-KEY: ((gcpServiceAccount.json))
